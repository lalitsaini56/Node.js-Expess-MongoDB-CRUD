<h3>{{viewTitle}}</h3>

<form action="/adminlogin" method="POST" autocomplete="off">
    <input type="hidden" name="_id" value="{{adminlogin._id}}">
    <div class="form-group">
        <label>Email</label>
        <input type="text" class="form-control" name="email" placeholder="Email" value="{{adminlogin.email}}">
        <div class="text-danger">
            {{adminlogin.emailError}}</div>
    </div>
    <div class="form-group">
        <label>Password</label>
        <input type="password" class="form-control" name="password" placeholder="Password" value="{{adminlogin.password}}">
        <div class="text-danger">
            {{adminlogin.passwordError}}</div>
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-info"><i class="fa fa-database"></i> Submit</button>
    </div>
</form>